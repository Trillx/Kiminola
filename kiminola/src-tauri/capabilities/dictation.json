{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dictation",
  "description": "Dictation state events for the nonactivating edge indicator",
  "windows": ["dictation"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
